# Ruzzle


## Installation

> [!WARNING]
> In order to install the project, you will need `CMake` and `Rust` installed

1. Clone the repository
```
git clone --recurse-submodules https://github.com/byxis/Ruzzle.git
cd Ruzzle
```

2. Launch the project
```
cargo build
cargo run
```
